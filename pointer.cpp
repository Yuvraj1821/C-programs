pointer : it is an varible which use to store the address of another variable
	
	syntax:
		
		datatype *variable_name;
		
	e.g, 
		int a,*ptr;
		ptr=&a;
		
		* To print the address of variable we have to use (%u) format.
		* Pointer varible can also be called as reference variable.
		* to print value of local variable by pointer varible,
		 	then we have to write / use pointer variable with astric symbol (*)
		 	
